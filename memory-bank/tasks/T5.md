# T5: Sync History Log

*Created: 2026-08-17 14:20 IST*
*Last Updated: 2026-09-01 22:45 IST*

**Status**: 🔄 **IN PROGRESS — logging and rotation partially implemented; structured errors and proper viewer pending**
**Priority**: P1 (User's #2)

## Details

Persistent log of all sync operations so users can answer "what just happened?" after a sync.

## Goals

1. **Log all sync operations**: timestamp, operation type (upload/download/delete/conflict), file path, success/failure
2. **Persistent storage**: Store in `.syncit/log.jsonl` (JSON Lines format — append-only, human-readable)
3. **UI access**: View log from sidebar or command
4. **Log rotation**: Keep last 30 days, auto-purge older entries
5. **Size control**: Let users set a maximum log size and rotate/purge safely at that limit
6. **Error details**: Capture full per-file error messages for debugging
7. **Reconciliation decisions**: Record first-sync authority, per-file decisions, tombstones, and manifest commits

## Current Source State

- `src/logging/SyncLogger.ts` already provides JSONL persistence, level filtering, age-based purge, size-based purge, and an optional plugin-directory backup.
- `src/settings.ts` already exposes log level, maximum age, maximum size (default 10 MB), and backup settings.
- `src/main.ts` records sync lifecycle summaries, but detailed failures returned by `SyncPlan.executePlan()` are only sent to `console.error`; they are not persisted as individual `ERROR` entries.
- `src/ui/SyncSidebarView.ts` contains a custom grouped list with a 500-entry read limit. It is not yet the proper Activity/Errors viewer described by the current plan.
- Rotation and runtime backup-setting changes need focused tests; the backup path is currently derived at logger construction time.

## Subtasks

- [x] SyncLogger class and JSONL persistence — implemented in `src/logging/SyncLogger.ts`
- [ ] T5a: Persist one structured `ERROR` entry for every failed sync operation
- [ ] T5b: Verify and harden age/size rotation, maximum-size settings, and backup toggling
- [ ] T5c: Add bounded log access for filtering, session grouping, and large logs
- [ ] Add a dedicated "View Sync Log" command if the redesigned viewer needs one
- [ ] Record reconciliation decisions and manifest/tombstone events

## Files
- Existing: `src/logging/SyncLogger.ts` — structured JSONL logger and rotation
- Existing: `src/ui/SyncSidebarView.ts` — current embedded activity viewer; to be replaced/refactored under T16
- Modify: `src/sync/SyncPlan.ts` — expose structured operation failures
- Modify: `src/main.ts` — persist per-file failures and wire the viewer
- Modify: `src/settings.ts` and `src/types.ts` — retain and verify log controls
- Tests: `tests/` — logger, error persistence, rotation, and filter coverage

## Notes
- JSONL format is append-only and easy to grep
- Keep it lightweight — no database, no complex indexing
- Log should survive plugin updates
- T13 depends on this log to make first-sync and deletion decisions auditable across devices
- T16 owns the user-facing redesign of the Activity and Errors surfaces; T5 owns the logging data contract and retention behavior.
