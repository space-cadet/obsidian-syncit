# T5: Sync History Log

*Created: 2026-08-17 14:20 IST*

**Status**: 📋 **PLANNED**
**Priority**: P1 (User's #2)

## Details

Persistent log of all sync operations so users can answer "what just happened?" after a sync.

## Goals

1. **Log all sync operations**: timestamp, operation type (upload/download/delete/conflict), file path, success/failure
2. **Persistent storage**: Store in `.syncit/log.jsonl` (JSON Lines format — append-only, human-readable)
3. **UI access**: View log from sidebar or command
4. **Log rotation**: Keep last 30 days, auto-purge older entries
5. **Error details**: Capture full error messages for debugging

## Subtasks

- [ ] Create SyncLogger class
- [ ] Add logging calls to all sync operations
- [ ] Create log file with rotation
- [ ] Add "View Sync Log" command
- [ ] Add log viewer UI (simple list in modal or sidebar)
- [ ] Add setting for log retention days

## Files
- New: `src/sync/SyncLogger.ts`
- New: `src/ui/LogViewer.ts`
- Modify: `src/sync/SyncPlan.ts` — add logging calls
- Modify: `src/main.ts` — add view log command

## Notes
- JSONL format is append-only and easy to grep
- Keep it lightweight — no database, no complex indexing
- Log should survive plugin updates
