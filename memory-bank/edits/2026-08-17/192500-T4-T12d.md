#### 19:25 IST - T4+T12d: ETag support and local sync index
- **Action**: Created, Modified
- **Files**:
  - `src/remote/WebDAVAdapter.ts` — parse `<d:getetag/>` from PROPFIND
  - `src/sync/SyncIndex.ts` — new `SyncIndexManager` class
  - `src/types.ts` — added `etag` to `FileEntity`, `SyncIndex`/`SyncIndexEntry` interfaces
  - `src/sync/SyncPlan.ts` — skip unchanged files via `indexManager.isUnchanged()`
  - `src/main.ts` — load index before sync, update after, clear on settings change
- **Details**: Unchanged files skipped on sync #2+. Index invalidated on server config change. Partial syncs update index incrementally via `patchIndex()`.
- **Commits**: `92a8d51`, `c6e1297`
